[build-system]
requires = ["meson-python>=0.15", "Cython>=3.0", "numpy>=1.24", "meson", "ninja"]
build-backend = "mesonpy"

[project]
name = "polyxios"
version = "0.5.0.dev0"
description = "Fast 3D file format I/O and processing library"
readme = "README.md"
requires-python = ">=3.12"
license.file = "LICENSE"
authors = [
    { name = "Serge Koudoro" },
    { name = "Praneeth Shetty" },
    { name = "Maharshi Gor" },
]
maintainers = [{ name = "polyxios developers" }]
keywords = [
    "mesh",
    "3d",
    "io",
    "vtk",
    "ply",
    "stl",
    "obj",
    "gmsh",
    "abaqus",
    "nastran",
    "finite-element",
    "scientific-computing",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: BSD License",
    "Operating System :: OS Independent",
    "Programming Language :: Cython",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: File Formats",
    "Topic :: Multimedia :: Graphics :: 3D Modeling",
    "Topic :: Scientific/Engineering",
    "Topic :: Scientific/Engineering :: Visualization",
]
dependencies = ["numpy>=1.24"]

[project.urls]
Homepage = "https://polyxios.org"
Documentation = "https://polyxios.org/stable/"
Changelog = "https://polyxios.org/stable/changelog.html"
Issues = "https://github.com/fury-gl/polyxios/issues"
Source = "https://github.com/fury-gl/polyxios"

[project.optional-dependencies]
dev = [
    "pytest>=7",
    "mypy",
    "cython>=3.0",
    "pre-commit",
    "spin",
    "ruff",
    "codespell",
]
doc = [
    "sphinx",
    "numpydoc",
    "sphinx-gallery",
    # Pinned to a minor series: 0.16 replaced the sidebar toggle markup
    # (checkbox+label -> details/summary), which _static/css/retro.css
    # targets. Bump deliberately and re-check the theme overrides.
    "pydata-sphinx-theme>=0.20,<0.21",
    "sphinx-design",
    "sphinx-copybutton",
    "sphinxext-opengraph[social-cards]",
    "sphinx-sitemap",
]
viz = [
    "fury>=2",
]
# The HDF5-backed formats: XDMF's usual heavy data, and MED, CGNS, H5M and
# HMF outright. Without it XDMF still reads and writes its inline and
# raw-binary flavours, and every HDF5 file or reference is refused with the
# install line that fixes it.
hdf5 = [
    "h5py>=3.0",
]
# Exodus II is a netCDF file. Without it the format is refused with the
# install line that fixes it.
netcdf = [
    "netCDF4>=1.6",
]
# LAZ, the compressed LAS. A plain .las needs nothing; a compressed one is
# refused without it, with the install line that fixes it.
laz = [
    "lazrs>=0.5",
]

[project.scripts]
pxios = "polyxios.cli:main"

[project.entry-points."polyxios.codecs"]
# third-party packages register their own entry points here

[tool.spin]
package = "polyxios"

[tool.spin.commands]
"Setup" = [".spin/cmds.py:setup"]
"Build" = ["spin.cmds.meson.build", ".spin/cmds.py:install"]
"Test" = [".spin/cmds.py:test", ".spin/cmds.py:lint"]
"Docs" = [".spin/cmds.py:docs"]
"Clean" = [".spin/cmds.py:clean"]
"Release" = [".spin/cmds.py:release"]

[tool.pytest.ini_options]
addopts = "--durations=10 -v"
testpaths = ["tests"]
# A warning the suite does not account for is a bug in the test or the code it
# covers, so it fails the run. Codecs warn a lot by design; every one of those
# belongs inside a `pytest.warns`, and nesting two catches a call that warns
# twice.
filterwarnings = ["error"]
markers = [
    "network: downloads assets from the remote data catalog (deselect with -m 'not network')",
]

[tool.ruff]
line-length = 88
target-version = "py312"
force-exclude = true
extend-exclude = ["__pycache__", "build", "docs/"]

[tool.ruff.lint]
# Explicit selection: ruff >= 0.16 widened its default rule set considerably,
# so rely on `select` rather than `extend-select` to keep the set stable.
select = ["E4", "E7", "E9", "F", "B", "C4", "I", "UP", "W", "PERF"]
ignore = ["B905", "C901"]

[tool.ruff.lint.isort]
known-first-party = ["polyxios"]
combine-as-imports = true
force-sort-within-sections = true

[tool.ruff.format]
quote-style = "double"

[tool.codespell]
# 'domin domout' is the Netgen .vol surface-element column header, spelled that
# way by the format. Scoped to the pair so a bare 'domin' is still a typo.
ignore-regex = 'domin\s+domout'
# 'NOE' and 'COO' are the node group and coordinate dataset of a MED file,
# spelled that way by the MED library.
ignore-words-list = "noe,coo"

[tool.mypy]
strict = true
python_version = "3.12"
ignore_missing_imports = true

[tool.cibuildwheel]
build = "cp312-* cp313-* cp314-*"
skip = ["*-musllinux_*", "*-win32"]

[tool.cibuildwheel.macos]
before-all = "curl -fsSL https://github.com/dipy/dipy_data/releases/download/v0.1/libomp-19.1.5-darwin20.tar.gz | sudo tar xz -C /"
environment = {CC = "clang", MACOSX_DEPLOYMENT_TARGET = "11.0"}
